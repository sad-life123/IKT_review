SQL files for IKT review.

`collect/` contains PostgreSQL collect steps used by `local_ikt_review\manager`.
Metrics are calculated from `local_ikt_review_snap` in PHP and stored in
`local_ikt_review_metric`.
